"use client";

/**
 * Workspace shell placeholder for CoderXP M2 Workspace Alpha.
 *
 * Commit 1 scope: placeholder only. The real workspace shell
 * (file explorer, editor, preview, runtime controls) will be
 * implemented in subsequent commits.
 */

export default function WorkspaceShell() {
  return (
    <div className="flex items-center justify-center min-h-[60vh] text-gray-400 text-sm font-mono">
      <div className="text-center space-y-2">
        <p className="text-white font-semibold">CoderXP Workspace</p>
        <p className="text-xs">M2 Workspace Alpha — foundation loaded</p>
      </div>
    </div>
  );
}
